# ZSH options
setopt nonomatch # I forgot what this does but I remember I'm used to it already, so keeping that (something about the wildcard glob)

# Aliases
alias z-config="vi ~/.zshrc"
alias z-history="vi ~/.zsh_history"
